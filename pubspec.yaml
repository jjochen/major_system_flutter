name: major_system
description: A new Flutter project.

publish_to: 'none'

version: 2.0.0+2

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  numbers_repository:
    path: packages/numbers_repository
  authentication_repository:
    path: packages/authentication_repository
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
  